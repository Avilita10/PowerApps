App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =LoadData(
            coleccionOffline,"Cache",true
        );
        LoadData(
            coleccionOffline2,"Cache2",true
        );
        LoadData(
            ColeccionOffline3,"Cache3",true
    StartScreen: =
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

